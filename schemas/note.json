{
    "description": "Note",
    "type": "object",
    "properties": {
        "display": {
            "type": "array",
            "items": {
                "type": "string",
                "enum": ["online", "inPerson"]
            }
        },
        "note": {
            "description": "Vanilla markdown",
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["display", "note"]
}
