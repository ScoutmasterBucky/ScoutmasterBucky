{
    "description": "Note",
    "type": "object",
    "properties": {
        "note": {
            "type": "array",
            "items": {
                "type": "string",
                "enum": ["online", "inPerson"]
            }
        },
        "text": {
            "description": "Vanilla markdown",
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["note", "text"]
}
