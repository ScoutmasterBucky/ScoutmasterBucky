{
    "type": "object",
    "properties": {
        "checkbox": {
            "description": "When checking the checkbox, what does that mean? Is it 'Approved'?",
            "default": "Approved",
            "type": "string"
        },
        "signature": {
            "description": "Who needs to sign",
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": ["signature"]
}
