{
    "name": "iles-app",
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "build": "iles build",
        "now": "run build && npm run preview",
        "preview": "iles preview --open --port 5050",
        "reinstall": "rm -rf node_modules package-lock.json && npm install",
        "start": "iles dev --open",
        "typecheck": "vue-tsc --noEmit"
    },
    "engines": {
        "node": ">= 20.0.0"
    },
    "devDependencies": {
        "@sveltejs/vite-plugin-svelte": "^4.0.0",
        "iles": "^0.10.0-beta.1",
        "typescript": "^5.6",
        "vue-tsc": "^2.1"
    },
    "dependencies": {
        "@modyfi/vite-plugin-yaml": "^1.1.0"
    }
}
